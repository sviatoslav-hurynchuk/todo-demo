import { Component } from '@angular/core';

@Component({
  selector: 'app-task-list',
  template: `
    <div class="task-list-container">
      <h2>Microsoft To-Do Tasks</h2>
      <!-- Tasks list, search bar, category filter, and pagination -->
    </div>
  `
})
export class TaskListComponent {}
